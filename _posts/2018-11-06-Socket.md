---
layout: post
title: "Socket"
author: "Xhy"
categories: dream
tags: [basketball]
image: joshua-ness.jpg
---


Photo by joshua ness

>96 hours of unbounded special training in 2018 to break the weak points

<br />

```c++
#include <iostream>
#include <string>

#include <winsock2.h>
#include <ws2tcpip.h>

#pragma comment(lib, "Ws2_32.lib")

#define SERVER_PORT 22992

#define RECV_BUFF_LEN 216

using namespace std;

void main()
{
    //1. Initialize the Winsock
    WSADATA wsaData;
    int iResult = WSAStartup(MAKEWORD(2, 2), &wsaData);
    if (iResult != 0) {
        cerr << "WSAStartup failed: " << iResult << endl;
        return;
    }

    //2. Create a socket
    SOCKET ListenSocket = socket(AF_INET,SOCK_STREAM, IPPROTO_TCP);  // 0
    if (ListenSocket == INVALID_SOCKET) {
        cerr << "Error at socket(): " << WSAGetLastError() << endl;
        WSACleanup();
        return;
    }

    //3. Bind the the ip and port to the socket
    sockaddr_in hint;
    hint.sin_family = AF_INET;
    hint.sin_addr.S_un.S_addr = INADDR_ANY;
    hint.sin_port = htons(SERVER_PORT);

    // Setup the TCP listening socket
    iResult = bind(ListenSocket, (sockaddr*)&hint, (int)sizeof(hint));
    if (iResult == SOCKET_ERROR) {
        cerr << "bind failed with error: " << WSAGetLastError() << endl;
        closesocket(ListenSocket);
        WSACleanup();
        return;
    }

    //4. Tell the winsock the socket is for listening
    if (listen(ListenSocket, SOMAXCONN) == SOCKET_ERROR) {
        cerr << "Listen failed with error: " << WSAGetLastError() << endl;
        closesocket(ListenSocket);
        WSACleanup();
        return;
    }

    cout << "Star server success..." << endl;

    //5. Wait for a client connection
    sockaddr_in clientAddr;
    int clientAddrLen = sizeof(clientAddr);

    SOCKET ClientSocket = accept(ListenSocket, (sockaddr*)&clientAddr, &clientAddrLen);
    if (ClientSocket == INVALID_SOCKET) {
       cerr << "accept failed: " << WSAGetLastError() << endl;
       closesocket(ListenSocket);
       WSACleanup();
       return;
    }

    DWORD dwRetval;

    char hostname[NI_MAXHOST];
    char servInfo[NI_MAXSERV];

    ZeroMemory(hostname, NI_MAXHOST);
    ZeroMemory(servInfo, NI_MAXSERV);

    dwRetval = getnameinfo((sockaddr*)&clientAddr, clientAddrLen, hostname, NI_MAXHOST, servInfo, NI_MAXSERV, NI_NUMERICSERV);
    if (dwRetval != 0) {
       inet_ntop(AF_INET, &clientAddr.sin_addr, hostname, NI_MAXHOST);
       cout << "hostname = " << hostname << " connetced on port " << ntohs(clientAddr.sin_port) << endl;
       cerr << "getnameinfo failed with error # " << WSAGetLastError() << endl;
    } else {
       cout << "getnameinfo returned hostname = " << hostname << " servInfo = " << servInfo << endl;
    }

    //6. Close the listening socekt(Server socket)
    closesocket(ListenSocket);

    //7. While loop: accept and enco back to client
    char recvbuf[RECV_BUFF_LEN];

    while (true)
    {
        ZeroMemory(recvbuf, RECV_BUFF_LEN);
        int bytesReceived = recv(ClientSocket, recvbuf, RECV_BUFF_LEN, 0);
        if (bytesReceived == SOCKET_ERROR) {
            /*cerr << "recv failed with error: " << WSAGetLastError() << endl;
            closesocket(ClientSocket);
            WSACleanup();
            return;*/
            break;
        }

        if (bytesReceived == 0) {
            cout << "Client disconnected." << endl;
            break;
        }

        cout << "bytesReceived = " << bytesReceived << endl;

        // Echo the buffer back to the sender
        send(ClientSocket, recvbuf, bytesReceived+1, 0);

        //int bytesSend = send(ClientSocket, recvbuf, bytesReceived+1, 0); // why bytesReceived+1 !!!???
        //if (bytesSend == SOCKET_ERROR) {
        //    cerr << "send failed with error: " << WSAGetLastError() << endl;
        //    closesocket(ClientSocket);
        //    WSACleanup();
        //    return;
        //}

        //Send the bytesSend message
        //char byteMsg[512];
        //ZeroMemory(byteMsg, 512);

        //string
        //string sendMsg = "Send " +

        //int bytesSend = send(ClientSocket, recvbuf, bytesReceived + 1, 0); // why bytesReceived+1 !!!???
        //if (bytesSend == SOCKET_ERROR) {
        //    cerr << "send failed with error: " << WSAGetLastError() << endl;
        //    closesocket(ClientSocket);
        //    WSACleanup();
        //    return;
        //}

    }

    //8. Close the client socket
    closesocket(ClientSocket);


    //9. Clean up the winsock
    WSACleanup();

}
```



<br />
